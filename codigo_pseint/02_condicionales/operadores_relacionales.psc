Algoritmo OperadoresRelacionales
	
	//Operadores relaciones
	//== es igual a 
	// === Es exactamente igual a (JS) y PHP
	// > Mayor que
	// > Menor que
	// >= Mayor o igual que
	// <= Menor o igual que
	// != Diferente de 
	
FinAlgoritmo
